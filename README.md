# Fibonacci Generator

This repository contains a Python program that generates Fibonacci numbers using a generator function (`yield`).  
It allows the user to input how many Fibonacci numbers they want to generate.

## How to Use
1. Clone this repository
2. Run the script:
   ```bash
   python fibo_generator.py
